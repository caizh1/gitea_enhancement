# Gitea 增强源码说明

本仓库从官方 Gitea `v1.27.3` 基线提取本地治理、审计、审批、权限继承和相关兼容性增强源码。

## 仓库边界

- 包含正式源码、模板、前端资源、测试、构建文件和维护文档。
- 不包含本地 `work/` 运行现场、`outputs/` 离线包与镜像、构建日志、测试数据库、临时凭据和逐次验收原始记录。
- `tests/integration/private-testing.key` 是官方 Gitea 基线已跟踪的测试夹具，不是本地生产密钥。

## 验收边界

本次提交用于保存和协作开发源码，不等同于生产发布或完整生产验收。部署前仍需按目标环境重新执行构建、迁移、回归、安全和并发验证。
