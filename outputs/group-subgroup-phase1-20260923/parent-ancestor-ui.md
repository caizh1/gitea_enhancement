# 第二批真实浏览器与 Runner 证据

日期：2026-09-23；本机独立实例 `127.0.0.1:13043`，SQLite，普通账号 `phase1-owner`（非实例管理员）。运行构建 `gitea-phase1-ancestors-v1`，尚未包含后续必选 Runner 信任、受保护 Secret 与 Webhook 快照修复。本记录不代表那些后续修复已经验收。

## 实际操作与结果

1. 普通 Owner 从群组 UI 创建第三层群组 `phase1-parent/child/deep`、私有仓库 `ancestor-ci`，创建 Issue #1；父级 Issue 汇总显示该条目，切换仅本级后计数为 0，切换“我创建的”后刷新保留筛选及结果。
2. 在父群组 UI 创建变量 `PHASE1_LEVEL` 和测试 Secret，保存并刷新；Secret 只显示掩码。第三层设置页展示父级只读来源。凭据仅为隔离环境测试字符串，本记录不保存值。
3. 父组创建 `shared-workflows` 私有来源仓库，经真实 HTTP Git 提交统一工作流；来源配置在 UI 注册。第三层消费仓库本身没有 YAML。子组 Runner 通过真实任务接口执行第三层工作流，Run 17 push 与 Run 18 PR 的首次执行成功。
4. 经真实 HTTP Git clone、建立分支、提交与 push，从 UI 创建 PR #2。Actions 结果页显示来源工作流、工作步骤与成功日志，未输出 Secret 明文。
5. 第三层 UI 新增同名变量，值改为测试预期外值；重跑 Run 18，第二次尝试 Job 20 显示 Failure。UI 修正变量后重跑，第三次尝试 Job 21 显示 Success，刷新结果保留。祖先同名变量显示被覆盖。
6. 父组标签页新建标签“一期祖先标签”，来源 ID 为 1；第三层 Issue #1 标签选择器显示父组来源，选择后刷新仍关联，时间线记录添加标签。点击标签筛选后显示 1 条匹配 Issue。
7. 群组 Issue 汇总以 390×844 视口查看，导航正常换行，按钮与筛选无遮挡。DOM 测量 viewport=390、scrollWidth=390，无横向溢出。只覆盖此页与此数据，不代表全部窄屏完成。

## 尚未验证

本记录尚未覆盖兄弟树、共享成员、无关用户、匿名用户的真实浏览器反例；没有替代对应自动化权限测试。未验证新必选门禁、受保护 Secret、Webhook 接收端、OCI/包/LFS、完整十仓库样本以及人工效率指标。千仓目标 Linux amd64 性能环境由用户确认暂未提供，保持未验证。
