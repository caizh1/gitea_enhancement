# 原生审批统一验收与审查报告

验证日期：2026-09-16。环境：macOS 开发主机；独立 SQLite；Linux amd64 离线镜像通过 Docker 仿真运行。没有操作用户内网生产实例。

## 结果

源码专项 Go 测试覆盖治理模型、原生保护、评审、治理服务、合并服务、原生 API 和设置路由。整合真实 Git 回归通过，记录用时 95.089 秒。新增用例与既有回归检查规则计票、版本冲突、事务回滚、保护稳定关联、PR 不可覆盖原生约束、仅审批管理员不能修改原生保护，以及新旧 API 字段相同/冲突/省略语义。

真实 Git 回归覆盖引用事务、授权临界区撤回及源更新、不同合并方式、同差异 rebase、A→B→A、授权恢复、直接推送保护、提交者与重新认证。既有测试仍保留 Git 引用与权限断言；群组导航调整只更新对应原生路径、页签及测试上下文。

迁移测试验证原生 101 人要求不截断、名单保留、稳定 ID、历史快照不改写、可重复执行及错误关联全事务回滚。

离线镜像导出后移除本任务镜像标签，再从包内镜像归档重新加载启动。容器未挂载源码，程序报告 `1.27.3-approval-unified`，架构为 Linux amd64。使用新数据库创建独立组织、仓库、三名审批人及研发/质量团队：

1. 配置总人数 3、研发 1、质量 1，质量规则来自上级群组。
2. 两名研发批准后质量缺失，总人数仍缺 1；合并被拒绝，目标引用保持不变。
3. 同时属于研发和质量的第三人批准，两组满足，总人数为 3，不累加组票。
4. 撤回第三人批准后再次不满足；重新批准后合并成功，真实目标引用更新。

包内 Chromium 浏览器通过草稿取消无写入、保存后回读、并发修改返回 409 且草稿保留、390 像素宽度、实际自动深色样式、键盘 Escape 关闭及无页面脚本异常。桌面与窄屏截图已人工查看。最初仅修改主题标记的测试不能证明深色效果，已改成真实媒体主题并补充页面宽度检查。

## 设计对应

| 项目 | 结果 |
| --- | --- |
| 原生分支保护入口与审批表格 | 已实现，包内截图与操作验证 |
| 本地草稿和统一保存 | 已实现，事务与浏览器验证 |
| 上级来源、共享规则、仓库级设置 | 已接入原生设置；深层群组路由有集成覆盖 |
| PR 和最终合并统一计票 | 已实现，逐规则结果及引用更新验证 |
| 旧 API 兼容和旧地址鉴权跳转 | 已实现，集成覆盖 |
| 存量迁移 | 自动化样本通过；目标内网副本待验证 |
| Linux amd64 离线包 | 从归档加载的隔离实例已复验 |
| 目标内网环境、80% 人工指标 | 未验证 |

## 对抗式审查

VERDICT: PASS

P0/P1 BLOCKERS:

无。当前审查未发现符合阻塞判定标准的成立问题。

审查证据包括：完整保护回调在服务端独立要求仓库管理员；审批投影仅修改审批兼容列；版本摘要覆盖原生保护、本地规则、设置和上级规则；批量保存共享事务与审计；隐藏主体投影与保存保留；共享关联删除不会成为全分支；PR 自定义无法伪造或移除原生强制规则；原生重复计票入口移除而其他保护保留；最终 Git 引用授权仍执行；迁移异常阻止启动；保存成功但刷新失败不误报可重试写入。

反证检查：服务端能力检查不依赖菜单隐藏；原生兼容记录不能通过省略身份字段转成普通规则；旧字段省略在写入锁内重新取值；同一请求旧/新相同字段允许，冲突拒绝；历史证据不能绕过差异版本与重新认证；无 JavaScript 时隐藏初始版本化提交避免原生审批字段被默认清空。真实 Git 曾出现的新对象读取失败已定位到命令环境丢失并修复，保留失败日志和修复后记录。

UNVERIFIED RISKS:

- 目标内网数据库、身份源与原有 Hook 组合尚未用该包验收；PostgreSQL/MySQL 存量迁移未在本次隔离包运行。
- 多节点、高容量和长时间稳定性未覆盖。AGit 专有推送路径没有新增专项验收，不把普通 Git rebase 结果延伸成全入口证明。
- Firefox 与真实移动设备未验收。原生完整页面其他区域的所有无障碍操作尚未逐项检查。
- 管理员例外、全部 CI/签名/文件保护配置的笛卡尔组合未穷举；代码路径保留与专项回归不能替代目标实例组合验证。

NON-BLOCKING FINDINGS:

- 分支列表和编辑页已提示明确分支的候选不足及入口未接入；通配分支显示范围说明，实际 PR 才能精确给出作者、提交者和当前成员资格下的不足人数。
- 新审批文案当前以中文实现，其他语言沿用原生文案，截图为混合语言账号；完整翻译覆盖尚未完成。

## 人工成本

目标用户为仓库管理员、审批管理员和 PR 作者。输入为仓库分支保护、用户/群组/团队及规则来源。代表性固定样本是总人数 3、研发 1、质量 1，含交叉成员、撤回与继承。

拟比较任务：配置三条要求、修改人数并确认范围、定位无法合并原因。记录每项操作次数、跨页次数、耗时与人工介入；不计真实代码评审判断时间。尚无同一批操作者的前后测量，因此“减少至少 80%”标记为未验证，不以按钮减少推算实际收益。

## 证据位置

交付包 `evidence/` 包含关键测试输出、真实 Git 结果与截图。失败前后日志分别保留，日志中不包含账号密码。实际验收账号和密钥留在本机私有缓存，不进入包。
