# 角色、组织、权限与仓库联动实测报告

本轮由 3 个 Sol 子代理分组执行，主执行者补充并发、故障回滚、规模测试及结果反证。源码基线 `1d07c4e`；仅本机隔离实例，未改业务源码，未部署阿里云或生产。

**VERDICT: FAIL**。原因是已知的严格在途撤权目标缺口 `PERM-RACE-001`（P1）再次复现；它继承上游在途语义，不是已证增强引入回归。另有 4 项 P2，详见[问题登记](role-organization-repository-test-bugs-20260918.md)。

80 条设计场景均已触达，但只有 **4 条完整通过、9 条发现问题、67 条部分通过并保留缺口**。场景触达 100% 不等于全入口覆盖或通过 100%；本轮不能宣布完整验收通过。

主结果文件共有 465 条记录，筛选后 274 条有效证据（263 条通过、10 条失败、1 条部分通过），排除 191 条准备错误、被替代或边界记录。一条记录可能包含多个操作，该比例不作为独立用例通过率。OR 首轮另有明确的留存缺口，见后文。

## 已完成的重要业务与边界

- 真实邮件邀请入职 → 后代仓库开发 → PR → 一票拒绝 → 两票但缺最新检查拒绝 → 检查通过后合并。测试邮件仅由本机接收器捕获，没有向外部收件人发送。
- 直接、继承、共享、原生 Team/协作者来源的核心边界；升级、降级、到期、禁用和多来源撤权。真实浏览器完成成员新增、升级及降级。
- 最后永久 Owner 删除、降级、改到期均拒绝；并发自退保留一名 Owner。撤管理权后的已排队成员修改拒绝；目标先归档后移组拒绝。
- 组织跨根移动、新旧权限核验、移动前邀请的真实邮件与浏览器接受；归档撤权恢复、延迟删除恢复及专用子树永久删除。E06 的审批候选重算至最终合并仍未验证。
- 审批组直接成员资格、同一账号多组去重、撤组重算；负责人完成受保护分支合并及标签发布。引用/PR/标签正确，但通用 main push 动态缺失再次复现。
- 独立双应用进程共享数据库的旧 Token 撤权、次进程重启；审计写入故障导致权限修改整事务回滚。不能替代不同物理主机和全协议组合。

## 固定规模结果

200 个组织、2,000 个仓库、1,000 名成员，常规深度 10：分页精确匹配应见的 1,000 个仓库，无漏项、重复和越界。深度 20 创建成功，21 拒绝且无残留；十人成员批次变更、自然到期、移出十仓库后 1000→990→恢复1000 均符合预期。

分页 P95 **9.623 秒**，超过执行前冻结的 **2 秒**目标，登记 `PERF-LIST-001`（P2）。环境是共享 Docker 18 CPU、约 8 GiB；资源只做抽样，不能给出峰值或生产容量结论。200 个仓库有初始化内容，其余为空仓。准备中断后先对账已提交对象；一个被历史兼容别名占用的失败夹具使用不同名称补足规模，原始恢复失败仍登记。

## 80 条逐行结果与未验证项

“完整通过”仅指该行声明范围。所有“部分通过”均不能用于签署整行通过；HTTP 网页请求与 Playwright 实际点击分别记录。原始[设计矩阵](role-organization-repository-test-matrix.md)保留每条预期，未为迎合实现而修改预期。

| ID | 本轮结论 | 已测内容 | 未验证/限制 | 问题 |
|---|---|---|---|---|
| R01 | 部分通过 | 私有仓库详情 API；HTTP/Token 与 SSH 克隆、真实提交推送拒绝；引用不变 | 网页搜索、详情和设置入口未实测 | 无已确认问题 |
| R02 | 部分通过 | Minimal Access 的 HTTP/SSH 代码读写拒绝；组织成员写接口拒绝且修订号不变；业务补测已验证组织可见而事项、PR API 拒绝 | 网页入口未实测 | 无已确认问题 |
| R03 | 部分通过 | Guest 的 HTTP/SSH 代码读写拒绝、成员管理拒绝；业务补测已验证本人事项创建编辑成功、管理他人事项拒绝 | 网页入口未实测 | 无已确认问题 |
| R04 | 发现问题 | Planner HTTP/SSH 代码读写拒绝；固定 PR 的元数据 API、diff 入口；E04 同账号管理他人事项成功；业务补测覆盖事项管理 | PR 元数据网页未实测；补丁、提交内容未分别实测 | CAP-PR-001 |
| R05 | 部分通过 | Reporter HTTP/SSH 克隆允许、真实推送拒绝且引用不变、成员管理拒绝；业务补测覆盖本人事项、他人事项管理拒绝、Wiki 和发布读取 | 合并入口和网页未实测 | 无已确认问题 |
| R06 | 部分通过 | Developer HTTP/SSH 克隆及功能分支真实推送成功；成员管理拒绝 | PR 创建、审批和受保护分支合并未在本脚本完成；网页未实测 | 无已确认问题 |
| R07 | 部分通过 | Maintainer HTTP/SSH 开发成功；API 创建后代组织成功；组织成员修改拒绝且状态不变；业务补测覆盖建库、仓库设置和仓库成员增删 | 顶层设置和网页未实测 | 无已确认问题 |
| R08 | 部分通过 | Owner HTTP/SSH 开发成功；本组织成员管理成功；无关联 beta 管理拒绝且状态不变 | 后代治理全入口和受保护 main 未在本脚本执行；网页未实测 | 无已确认问题 |
| R09 | 部分通过 | Reporter 加 manage_approvals 角色创建分配；代码读取保留、推送拒绝；审批设置 API 成功 | 实际 PR 审批规则与合并结果未闭环；网页未实测 | 无已确认问题 |
| R10 | 部分通过 | 无管理权创建 Owner 角色拒绝；未知能力拒绝；修订号不变 | 有管理权但授出超出自身能力的独立账号变体未另测；网页未实测 | 无已确认问题 |
| R11 | 部分通过 | 同一角色增加 manage_members 后原会话成员写成功；移除后原会话新请求拒绝；Reporter 基础代码读取保留 | 第二个引用同角色的成员同步生效未另测；后代资源和网页未实测 | 无已确认问题 |
| R12 | 部分通过 | 成员引用期间删除和基础角色修改均拒绝且修订号不变；解除成员引用后删除成功 | 未过期邀请引用和未使用角色合法修改变体未完成；网页未实测 | 无已确认问题 |
| O01 | 完整通过 | 网页创建子组织；API创建下一级组织；HTTP Token克隆/推送；SSH用户密钥克隆/推送；父ID/完整路径/直接成员核对 | 无 | 无已确认问题 |
| O02 | 部分通过 | API篡改父组ID；目标树前后集合 | 无权限用户未通过真实网页篡改隐藏parent_id | 无已确认问题 |
| O03 | 部分通过 | API可见性；HTTP Token开发；SSH用户密钥开发 | 组织/仓库页面未逐页截图核对 | 无已确认问题 |
| O04 | 部分通过 | 本级/后代HTTP+SSH允许；父级/同级/无关HTTP+SSH拒绝 | 页面入口未逐路径浏览 | 无已确认问题 |
| O05 | 部分通过 | 两账号反向高低角色组合；sdk/business HTTP+SSH | 页面角色来源解释未核对 | 无已确认问题 |
| O06 | 部分通过 | API后代环；冲突名称；非法路径；回滚后路径 | 非法移动未通过网页表单执行 | 无已确认问题 |
| O07 | 部分通过 | API跨根移动；旧父/新父/直接成员HTTP+SSH；稳定引用 | 组织移动网页预览/确认未执行；邀请/申请路径未建立 | 无已确认问题 |
| O08 | 部分通过 | API合法移动回执；移动后读取；移动后manage_group拒绝 | 网页移动回执未执行 | 无已确认问题 |
| O09 | 部分通过 | API改名；新旧API路径稳定ID | 网页旧链接与原生兼容路径未逐项浏览 | 无已确认问题 |
| O10 | 部分通过 | 最终私有可见性；匿名API；无权列表/计数隐藏 | 导航树、面包屑、分页和搜索页面未完整执行 | 无已确认问题 |
| M01 | 部分通过 | 添加 Developer；后代仓库 HTTP/SSH 克隆推送成功；beta HTTP/SSH 读写拒绝 | 成员列表中的来源、角色和到期显示及网页未实测 | 无已确认问题 |
| M02 | 部分通过 | 仅 manage_members 的 Maintainer 添加仓库成员成功；组织成员接口拒绝且修订号不变；根R07另有同能力Maintainer仓库成员删除对照 | 仓库成员删除 API 和网页未实测 | 无已确认问题 |
| M03 | 部分通过 | Guest 加 manage_group_members 后给他人 Developer、修改 Owner 均拒绝且修订号不变 | 给自己授 Developer/Owner 未单列；网页未实测 | 无已确认问题 |
| M04 | 完整通过 | 同一旧 Token/SSH 会话 Reporter 只读、升级 Developer 后 HTTP/SSH 推送成功、降级后读取保留且推送拒绝；Playwright 使用真实账号密码登录并在成员管理页新增 Reporter、升级 Developer、降级 Reporter，三次 303 后由 API 独立见证最终直接角色 Reporter | 无 | 无已确认问题 |
| M05 | 部分通过 | 父组和仓库直接 Developer 两种撤销顺序；撤一条后 HTTP/SSH 仍可读写；最后来源撤销后读写拒绝且引用不变 | 成员页面来源解释未实测 | 无已确认问题 |
| M06 | 部分通过 | 治理成员、原生 Team、协作者三来源；逐条撤销后剩余能力保留；全部撤销后 HTTP/SSH 拒绝 | 三种撤销顺序未做全排列；网页未实测 | 无已确认问题 |
| M07 | 部分通过 | 30 秒独立窗口内 HTTP/SSH 到期前克隆推送成功；服务端到期后同一旧 Token/SSH 新请求均拒绝，不等待清理任务 | 到期边界同秒并发和成员页面有效状态未实测 | 无已确认问题 |
| M08 | 部分通过 | 专用组先添加两名非站点永久 Owner，再移除管理员原生 Owners 团队来源；删除、降级、改到期三种最后永久 Owner 请求均拒绝且修订号不变；恢复第二永久 Owner 后降级成功 | 继承 Owner 变体和网页未实测 | 无已确认问题 |
| M09 | 部分通过 | 同数据库3002次进程经本机STARTTLS测试域SMTP真实投递；接受前 HTTP/SSH 拒绝；真实令牌错误账号拒绝；正确账号接受后继承后代 Developer 并完成 HTTP/SSH 开发；重复接受拒绝且不扩大授权 | 邀请网页接受入口未实测 | 无已确认问题 |
| M10 | 部分通过 | 分别使用真实捕获令牌验证邀请过期、管理员撤销、受邀人拒绝、邀请者失权；四种旧令牌接受均拒绝，HTTP/SSH 均无成员权限残留 | 邀请网页入口未实测 | 无已确认问题 |
| M11 | 部分通过 | 公开组织开启申请；非成员提交；申请本身不授 Developer；申请者自批拒绝；管理员批准；HTTP/SSH 开发成功；重复批准拒绝 | 撤回、管理员拒绝及网页未实测 | 无已确认问题 |
| M12 | 部分通过 | 同一账号禁用前 HTTP/SSH 开发成功；禁用期间旧 Token/SSH 读写拒绝；重新启用后仍有效授权恢复 | 停用前邀请、共享、审批三种独立来源未逐项实测；网页未实测 | 无已确认问题 |
| S01 | 部分通过 | 治理API建立Developer成员和Reporter仓库共享；HTTP Git克隆/推送；SSH Git克隆/推送；成员管理API负向；邻库详情API负向 | 浏览器共享创建表单；网页边界：未测提交 | 无已确认问题 |
| S02 | 部分通过 | Reporter成员与Developer共享上限；HTTP/SSH克隆成功、推送拒绝；成员管理及邻库拒绝 | Reporter共享交集的实际审批请求未单独执行；X05验证的是其他专用审批主体，不能替代本变体 | 无已确认问题 |
| S03 | 部分通过 | Developer交集；HTTP/SSH功能分支真实提交与推送；邻库和管理接口拒绝 | 网页边界：未测提交 | 无已确认问题 |
| S04 | 发现问题 | Planner/Reporter、Reporter/Planner、Planner/Developer三组；PR详情API；事项API及他人事项修改API；代码blob和补丁API；真实网页登录后的PR详情页与代码页GET | 网页事项编辑提交 | CAP-PR-001 |
| S05 | 部分通过 | Developer自定义角色额外manage_members；Developer共享上限；HTTP/SSH功能分支推送成功；成员管理API拒绝且前后状态不变 | 网页边界：未测提交 | 无已确认问题 |
| S06 | 部分通过 | 组织共享给邀请子组直接成员；仅父组继承成员组织共享负向；仓库共享采用邀请组有效GroupGrants正向；组织共享后代仓库网页GET；无关根邻库负向 | 明确原生Owners成员正向；网页边界：目标仓库页面GET 200；未测管理提交 | 无已确认问题 |
| S07 | 部分通过 | 直接Developer+共享Reporter；先撤直接后仅共享只读；全部撤销拒绝；仅直接时双协议可写 | 反向撤销顺序使用同对象能力对照，未单独留第二套对象；网页边界：未测提交 | 无已确认问题 |
| S08 | 部分通过 | 共享先于成员真实服务端到期；重新建立共享恢复；成员手动撤销后共享来源失效 | 成员自然到期先于共享的第二时钟样本；网页边界：未测提交 | 无已确认问题 |
| S09 | 部分通过 | 真实网页表单启用禁止外部共享；跨根新增共享API拒绝；同根共享API成功 | 篡改已有共享目标ID的网页表单；设置只限制新增、既有关系不追溯的独立样本 | 无已确认问题 |
| S10 | 部分通过 | A共享B、B共享C，两跳组织读取拒绝；仓库共享给B后C有效授权正向；自共享及反向成环拒绝；重复PUT不增加关系行 | 网页边界：未测提交 | 无已确认问题 |
| P01 | 完整通过 | 真实网页建库；API有权/无权建库；HTTP+SSH Git实体 | 无 | 无已确认问题 |
| P02 | 部分通过 | API篡改owner/目标路径；beta列表前后 | 无权网页建库/篡改uid未执行 | 无已确认问题 |
| P03 | 部分通过 | 跨命名空间同名；同命名空间冲突；大小写变体；非法名称 | 网页新建同名/非法名未执行 | 无已确认问题 |
| P04 | 部分通过 | 直接Developer HTTP+SSH；邻库拒绝；组织管理拒绝 | 组织成员与设置网页未执行 | 无已确认问题 |
| P05 | 部分通过 | 公开转私有匿名前后；私有祖先下公开请求；最终私有状态 | 仓库设置网页可见性切换未执行；浏览器旧缓存入口未核 | 无已确认问题 |
| P06 | 部分通过 | API改名；新旧路径稳定ID；事项；保护规则；Git引用 | 网页改名与旧链接推送未执行；成员/共享未建立 | 无已确认问题 |
| P07 | 部分通过 | API跨组织转移；目标同名冲突；稳定ID/Git | 网页转移影响预览/确认未执行；缺源权限变体未独立执行 | 无已确认问题 |
| P08 | 部分通过 | 旧新继承与直接成员HTTP+SSH；迁移后稳定仓库 | 跨根自定义角色映射、共享、邀请、申请未建立 | 无已确认问题 |
| P09 | 部分通过 | API归档/恢复；HTTP+SSH读取/推送；引用前后 | 网页/API写文件和合并写入口未执行；归档网页未执行 | 无已确认问题 |
| P10 | 部分通过 | API影响预览/整树归档/解除；归档期间撤权；独立归档/待删除状态；HTTP+SSH | 归档/恢复网页未执行 | 无已确认问题 |
| P11 | 部分通过 | API延迟删除/恢复；事项/引用；恢复前撤权；HTTP+SSH | 删除/恢复网页未执行；PR和附件未建立 | 无已确认问题 |
| P12 | 发现问题 | API无权永久删除；旧due重放；永久删除204；DB/namespace/Git/cleanup核对；残留alias；同名重建409 | 永久删除网页未执行；组同名重建未测 | LIFE-RECREATE-001 |
| X01 | 部分通过 | 独立共享仓库、直接仓库、无权仓库；个人仓库列表API；逐仓详情API；真实网页登录首页GET；根C08独立用户多页1000仓库集合/总数已核对 | 站内搜索及首页/组织页在规模下全部入口一致性未完整执行 | 无已确认问题 |
| X02 | 部分通过 | 把有权仓库revision用于无权仓库成员写入；目标成员列表前后状态不含被写用户 | 组织ID、角色ID、共享ID、邀请ID分别篡改；网页边界：未测提交 | 无已确认问题 |
| X03 | 部分通过 | 有资源权但read:repository Token读取成功、事项写入拒绝；全范围Token但无资源权详情拒绝；双侧满足对照成功 | 受限Token HTTP Git单独推送，本轮仅治理/仓库API；网页边界：不适用Token API；未测网页提交 | 无已确认问题 |
| X04 | 部分通过 | main保护规则API；Developer直接Git推main拒绝；功能分支Git推送成功；文件API写main拒绝 | Reporter/Maintainer/Owner全角色矩阵；网页编辑main；PR满足审批与检查后的合并正向；网页边界：未测网页编辑提交 | 无已确认问题 |
| X05 | 部分通过 | 父组继承成员审批不计指定子组；同一账号直接属于两个子组并审批；两个组规则均满足但独立人数仍缺1；合并403且PR及引用不变；撤出一个子组后旧审批资格即时重算失效；两名当前直接成员重新审批后成功合并；合并前后PR与main引用核对 | 网页边界：未测管理提交；审批和合并均为真实API与Git引用闭环 | 无已确认问题 |
| X06 | 部分通过 | 自定义manage_members能力经Developer共享上限截断；代码交付成功；成员管理API拒绝且状态不变 | 审批设置、组织成员、共享、审计设置四种不同单项自定义角色；网页边界：未测提交 | 无已确认问题 |
| X07 | 发现问题 | 无代码权三组PR/事项元数据API；代码blob与PR补丁API负向；真实PR网页GET与代码页GET | 附件、归档、比较、提交详情、Wiki Git逐资源；撤权后新内容；网页边界：PR页面可读、代码页拒绝；未测下载按钮提交 | CAP-PR-001 |
| X08 | 部分通过 | 显式read_audit自定义角色、Owner、无权用户、跨组织Owner；审计查询API；真实审计网页GET；响应秘密字段扫描 | 导出生成、轮询及下载二次鉴权；移组/转移/删除事件内容完整性；网页边界：有权页面200、无权页面404；未测导出表单提交 | 无已确认问题 |
| C01 | 部分通过 | 同revision成员与自定义角色并发写入：一成功一409，最终数据核对 | 共享上限并发、逐审计事件对账未另测 | 无已确认问题 |
| C02 | 部分通过 | 专用组织移除原生admin Owner后，两永久Owner并发自退，保留一名Owner | 同时降级对方的独立变体未另测 | 无已确认问题 |
| C03 | 部分通过 | 专用触发器确保撤组织Owner事务先提交，旧身份已排队仓库成员管理404，成员不变 | 共享更新与自定义角色更新在途变体未另测 | 无已确认问题 |
| C04 | 发现问题 | HTTP和SSH均在前置鉴权后暂停，撤唯一来源，新读拒绝但在途push成功，原生hook不变 | 纯上游二进制独立部署对照未重跑；上游归属沿用源码对比，不声称官方漏洞评级 | PERM-RACE-001 |
| C05 | 部分通过 | 同版本双独立应用进程共享DB/存储，次进程旧Token撤权新请求拒绝，次进程重启仍拒绝 | 不同物理主机、跨节点网页会话及Git各入口未执行 | 无已确认问题 |
| C06 | 部分通过 | 同revision移组与成员修改竞争；确定目标归档先提交后在途移组409；ID/代码/路径核对 | 仓库转移与共享竞争、目标永久删除竞争尚未构造 | 无已确认问题 |
| C07 | 发现问题 | 专用成员审计写入失败整事务回滚，同revision恢复成功；真实创建响应中断后DB与Git清理但兼容别名阻止同名恢复 | 创建/迁移/删除进程退出完整故障笛卡尔积未测；连接中断原因为未验证，不能算已知服务崩溃 | LIFE-RECREATE-001 |
| C08 | 发现问题 | 200组织2000仓库1000成员、深10；全部分页1000精确匹配；深20成功21拒绝；十人成员批次变更、自然到期、移动10仓库1000→990→1000；P95=9.623秒超过冻结2秒 | 仅当前共享18CPU约8GiB Docker；小样本是同大数据库中仅一仓库授权用户，非空DB基准；资源抽样非峰值，非生产容量保证 | PERF-LIST-001 |
| E01 | 部分通过 | 真实邮件 Developer 邀请接受；同一受邀人继承 sdk 开发并推送固定提交、创建 PR；一名独立审批人后合并拒绝，两名审批但缺最新检查仍拒绝；最新提交检查通过后合并成功，管理员绕过关闭 | 邀请接受和PR审批合并网页未实测 | 无已确认问题 |
| E02 | 部分通过 | 同一账号组织治理、仓库直接成员、协作者三来源；只移出组织成员来源后其他能力保留，逐条撤销至完全离职后 HTTP/SSH 拒绝；禁用后旧凭据继续拒绝 | 原生 Team 和共享来源未同时纳入该 E02 专属账号；网页未实测 | 无已确认问题 |
| E03 | 完整通过 | 同根供应商组Reporter短期仓库共享；有效期内HTTP/SSH克隆成功和推送拒绝；邻库和成员管理拒绝；服务端到期后详情拒绝 | 无；按该只读供应商链路主要声明范围完整，其他共享GUI变体见S组 | 无已确认问题 |
| E04 | 发现问题 | 同一 Planner 管理开发者创建的事项成功；固定 PR 元数据 API 返回 403；diff、HTTP/SSH 代码读写拒绝且引用不变 | PR 元数据网页未实测 | CAP-PR-001 |
| E05 | 发现问题 | 仓库Maintainer配置main保护、两人审批规则和最新检查API；授予Developer与两名独立审批人；Developer创建功能分支和PR；两人审批后缺检查仍拒绝合并；当前head检查成功后合并且main引用变化；Developer发布普通标签；Developer发布受保护release标签失败且负责人发布成功；PR状态、最终引用、action数据库、仓库动态API、post-receive日志、组织越权拒绝核对 | 真实浏览器管理表单提交；Webhook/Actions未配置，不能推断影响；网页边界：未测保护、审批和成员管理网页提交 | MERGE-EVENT-001 |
| E06 | 部分通过 | 同一团队跨根移动；旧新继承/直接成员HTTP+SSH；移动前打开PR；移动前邀请；SMTP真实投递；浏览器预览/接受；接受后HTTP+SSH | 审批策略和审批候选重算未测；原PR未推进到最终合并；合并后动态/审计未核 | 无已确认问题 |
| E07 | 部分通过 | 同一组归档/撤权/恢复；合法与失权用户HTTP+SSH | 共享撤销未建立；归档网页未执行 | 无已确认问题 |
| E08 | 部分通过 | 同一组延迟删除/恢复；事项/引用/当前授权；同根专用子树永久删除 | 外部共享未建立；组同名重建未执行 | 无已确认问题 |

## 证据、统计与留存说明

- [基线与二进制校验](evidence/governance-linkage-20260918/baseline.json)、[机器可读汇总](evidence/governance-linkage-20260918/summary.json)、[逐行覆盖](evidence/governance-linkage-20260918/coverage.json)。
- [有效证据及原行指针](evidence/governance-linkage-20260918/effective-records.jsonl)、[排除记录与理由](evidence/governance-linkage-20260918/excluded-records.json)。原始 [R/M](evidence/governance-linkage-20260918/rm-results.jsonl)、[O/P](evidence/governance-linkage-20260918/or-results.jsonl)、[S/X](evidence/governance-linkage-20260918/sx-results.jsonl)、[并发/规模及角色补证](evidence/governance-linkage-20260918/concurrency-results.jsonl)。
- OR 首轮 22 条中，16 条保留原行，6 条完整快照未保留，只有当轮状态日志与明确标注的重建摘要。重建摘要不是原始证据，最终判定依据独立补测。见[首轮日志](evidence/governance-linkage-20260918/or-initial-run.log)与[留存清单](evidence/governance-linkage-20260918/or-initial-results.jsonl)。
- 不提供不可靠的全量正向成功率：部分结果聚合正反操作，统一原子分母尚不具备。确定的在途越权放行为 HTTP/SSH 各一次，同一根因；PR 合法误拒绝覆盖四类能力组合，重建目标差异有两个独立样本。
- Windows/TortoiseGit 按用户要求跳过。本轮不重复计算此前 macOS GUI、Docker Remote-SSH 的历史结果；本次网页覆盖以表中实际操作为准。
- 测试暂停 Hook、数据库触发器及次应用/本机 SMTP 已清理；主服务仍健康，无受跟踪业务代码改动。保留隔离测试数据用于复现；没有声称减少人工 80% 或量化 Token 节省。

## 风险判定

P0：0；P1：1；P2：4。只有已证实的 P1 严格撤权目标缺口导致 FAIL。Planner API、分页性能、同名恢复契约差异及合并动态为 P2，不单独阻塞；未验证入口、间歇连接中断首因和实际 Webhook/Actions 交付边界不升级为缺陷。

当前结果支持“多项核心权限和正常业务链可运行”，不足以支持“全部联动无大风险”。后续应先处理严格撤权目标，再补表中的全入口和高风险迁移/并发缺口。
