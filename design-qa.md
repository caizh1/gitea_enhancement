# 协作者与群组共享验收记录

日期：2026-09-22。范围：当前工作区中的协作者页面合并、群组邀请弹窗与公共搜索修复。未发布安装包。

## 实现与设计核对

使用已选设计图 `outputs/collaboration-sharing/design-reference.png`，与实际页面 `desktop.png`、邀请弹窗 `invite-dialog.png` 一起查看比较。桌面截图为 1280×900；窄屏按 390×844 检查，页面可见宽度与滚动宽度均为 390。未把设计展示板外围或并排弹窗视作产品布局。

- 字体与层级：沿用产品字体、正文尺寸、标题和帮助文字；保留原协作者区的名称、权限和删除操作。
- 间距与布局：群组区位于协作者下方，桌面四列，窄屏单列显示字段标签，按钮自动换行。图标使用现有图标库，保持在 flex/grid 正常文档流中。
- 色彩：沿用当前 Gitea 主题变量和蓝色主按钮，不单独把这一页改成设计示意图中的绿色主题。其他导航入口保留现有产品功能。
- 图像：使用真实用户头像和原有图标；无新增装饰性图片。
- 文案：群组显示名称与完整路径，角色上限、UTC 有效日期、永久有效、撤销后其他权限来源不受影响等说明齐全。
- 交互：浏览器实际完成关键词搜索、选择群组、邀请、角色编辑、日期回填、保存、撤销二次确认和撤销成功；关闭按钮也已验证。空状态有操作说明。

首次检查发现原生下拉框宽度和帮助文字换行不符合弹窗布局，已改为全宽控件与独立帮助行，并重新截图检查。新增关闭按钮留在标题行正常文档流中。无未处理的 P0/P1/P2 视觉问题。设计中的计数徽标未加入：当前列表支持分页，避免把当前页数量误作总数；属于非阻塞展示差异。

final result: passed

## 自动化与实际协议验证

- `TestGovernanceRepositorySharingHTTP`：通过，最终运行 3.748 秒。覆盖旧入口跳转、协作者页面完整渲染、群组搜索、非 Owner 搜索和修改被拒绝、修改角色与日期、精确到期时刻保留、旧修订号冲突、跨站 POST 被拒绝、真实 Git 克隆成功、撤销后 API 与 Git 访问被拒绝。
- `TestRepositorySharingIncludesInheritedMembersAndRevokesOnlyItsSource` 与 `TestAddTeamPost_NotAllowed`：通过，验证继承成员和原群组授权保留，以及原团队权限边界。
- 公共搜索回归：修复前相同测试失败，修复后通过。根因是 debounce.cancel 默认永久取消搜索；现在只取消当次排队调用。覆盖外部点击、失焦、重新输入与候选选择。
- 前端构建、修改文件 ESLint、TypeScript 类型检查、CSS lint、修改范围 Go lint、diff 空白检查均通过。
- 全仓库 `make lint-js` 与 `make lint-go` 未通过，存在其他文件的检查问题；没有把专项通过表述为全仓库通过。原始输出在同目录对应日志中。
- 页面控制台在开发服务重启前后出现公共 worker 通道错误；邀请、编辑、撤销及页面呈现均已实际完成，未据此宣称整个站点控制台无错误。

详细机器日志与截图位于 `outputs/collaboration-sharing/`。本次验证为 macOS 隔离 SQLite 实例；不等同于新版 Ubuntu 离线包验收。

## 代码审查

VERDICT: PASS

P0/P1 BLOCKERS: 无。

已检查与反证：

1. 合并页面不扩大授权。列表、搜索和写入均复用 Owner 检查，目标组仍要求成员资格；非 Owner 的 HTTP 读写负例通过。
2. 客户端隐藏按钮或只读字段不作为授权依据；服务层仍检查角色、归档、跨根共享限制、修订号和审计。
3. 表单继续使用现有跨站来源保护；此版本 Gitea 使用 CrossOriginProtection，而非旧版 CSRF 隐藏令牌。跨站提交实际返回 403。
4. 跳转目标由数据库仓库链接构造，不接受任意返回 URL。
5. 撤销复用现有原子治理写入，真实 Git 与 API 撤权均验证；其他来源的群组权限在服务测试中保留。
6. 编辑日期按 UTC 回填，保留没有更改日期时原先 API 设置的精确秒数，避免只改角色却延长权限。
7. 模板自动转义群组名称与路径，搜索结果由既有安全 HTML 构造函数生成。
8. 公共搜索修复覆盖用户、仓库、团队与新增群组搜索的共用入口，不新增独立搜索框架。

UNVERIFIED RISKS:

- 超大群组规模下的搜索性能、超过 100 条结果的操作体验未实测；搜索复用现有导航查询，用户可缩小关键词或输入完整路径。
- 未重新生成或发布 Ubuntu 安装包，未进行本轮跨数据库、真实手机或全站回归。
- 未测量真实用户完成任务的人工耗时，因此不宣称达到减少 80% 人工操作的指标。本次明确完成的是单页管理闭环及其权限和协议验证。

NON-BLOCKING FINDINGS:

- P3：主题颜色、原生协作者区与计数徽标相对示意图的差异如上；保留现有产品风格与功能。
- 全仓库 lint 仍需独立清理；本次修改范围检查通过。
