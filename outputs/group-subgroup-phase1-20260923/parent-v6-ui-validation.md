# 第六版真实成员自退验收

日期：2026-09-23；隔离 SQLite UI，构建 SHA256 `9308ff85f8ad9ff76f9dd0ce30a05a5721e56838bec8562d9565a1c959100c1e`。本构建包含自退能力，尚未包含后续 Team 错误提示和无合法人类触发者的 CI 诊断修改。

## 自退闭环

合成账号 `phase1-outsider` 通过此前真实邮件邀请成为深层 group6 的直接 Reporter。第五版只能看到“在授权来源处管理”；第六版本人直接授权行出现“退出本群组直接授权”。展开后明确说明只撤销本级直接来源，祖先、共享和原生团队仍可能有效，最后永久 Owner 需先指定继任者。

点击确认退出后，页面提示直接授权已退出，并安全跳转“我的群组”；列表显示暂无可查看内容。随后同一已登录浏览器访问先前实际下载成功的 Run30 产物 URL，显示 404 Not Found。显式固定 `phase1-outsider` 并禁用 credential helper 的新 HTTP Git 请求退出 128／Repository not found。

只读数据库佐证：group6/user3 的直接治理成员记录为零，该用户在全部原生 OrgUser、TeamUser 中也没有残留。本人不会因为此次退出获得其它权限；测试未宣称已收回此前下载的 ZIP。

## 邀请撤销

子级 Owner9 从 group6 成员页“成员邀请”创建第二份 Reporter 邀请，收件人为已退出的合成用户。列表先显示待投递，后台按既有一分钟周期送至本机隔离 SMTP，随后刷新显示“已交给邮件服务器”及投递时间。接收器真实收到邮件。

子级 Owner 点击“撤销邀请”后，列表显示暂无待接受邀请。收件人再次打开该邮件的结果另行补充，不用数据库状态代替真实页面验证。

## CI 身份恢复与部署过程

第五版部署密钥推送生成 Run36 原始 Attempt39，其触发者是组织6；Runner 的最终身份检查拒绝该主体，原始作业一直等待。普通 Owner 在 UI 取消后明确点击重新运行，新的 Attempt40/Job40 以 Owner2 身份成功执行，UI 显示 Success、2秒、1个产物。此行为佐证现有安全重新授权路径，不把组织触发者无条件放行。

初次第六版切换预检查因原始等待作业而拒绝。父任务的外层命令当时没有在预检查失败后停止，意外尝试启动第二个第五版进程；该进程因既有队列锁失败退出，原监听进程未停止、固定入口未切换。保留首因记录；后续改为单个 Python 进程，仅在预检查、正常停止、SQLite backup 和入口切换全部成功后 `exec` 启动。处理等待作业后正常完成切换。没有删除队列、重置数据库或杀死原服务来绕过锁。
