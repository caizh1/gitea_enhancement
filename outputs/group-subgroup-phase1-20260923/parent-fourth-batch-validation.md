# 父代理第四批真实验收

日期：2026-09-23。测试实例只监听本机 `127.0.0.1:13043`，使用一次性测试账号、凭据和样本；不涉及生产数据。

## 构建边界

本批页面与协议使用 `gitea-phase1-ancestors-v4`，SHA-256 为 `0cf7042d326e4dd146e8a18e6052c4d80b8411569e1ad4d8e648bd7bcffaf013`。包含共享项目页、协作候选、通知过滤、最后人类 Owner、必选工作流诊断和移动影响预览；不包含正在修复的 Release/LFS 最终鉴权。v4 不是候选发布。

切换 v4 时首次备份指向错误目录，生成空库；已确认实际配置使用工作目录根部的 `phase1-ui.db`，原业务库没有被覆盖。正确的备份是启动 v4 后通过 SQLite backup 生成的 `after-v4-verified/phase1-ui.db`，不能称为启动前备份。错误空库已核对后清理，原因记录在隔离工作目录 `before-v4/backup-note.md`。

## 可信门禁、真实 Runner、审查与合并

1. 普通 Owner 从浏览器创建仓库 2 的 PR 3，源分支 `phase1-gate-ui`，初始提交 `2c70369e80cf5cc22a4b71c0dbded4e48b56c57b`；实际选择 `phase1-reader` 为审查人。
2. 根群组 Runner 2 执行 Run 24（push）和 25（pull_request）成功。根群组的必选模式从通配模式改为 `scoped:3: 一期祖先检查 / 验证祖先配置 (pull_request)` 后，旧成功仍展示，但页面明确无匹配当前配置的可信记录，合并按钮消失，并显示根群组修复入口。
3. 在真实临时 Git clone 新增提交 `47d0fcb07b1a1a269779ee7c2cab52b0eb3d4ee3` 并 HTTP push。Run 26/27 被根 Runner 2 执行成功；PR 页面恢复可合并及按钮。仓库自身没有 YAML，也没有 main 原生分支保护，祖先门禁仍实际生效。
4. Reporter 通过子群组 PR 汇总的“请求我审查”筛选找到该 PR，查看真实 diff 并提交审查；时间线保存审查意见。原生页面标记为不计数的批准（Uncounted approval），且明确 Reporter 无合并权。该正例不代表 Reporter 可以满足治理审批配额。
5. 普通 Owner 从 UI 合并成功，页面显示已合并，合并提交 `7c31cf76a925e356962f8df546807ca3a47f6f68`。其 push Run 28 也由根 Runner 2 成功执行。数据库只读检查佐证 Run 24–28 及 Task 22–26 均成功；不使用数据库写入替代工作流。
6. v4 旧诊断的“重跑工作流”措辞不够准确：代码保留旧 Run 的配置版本。子代理已将中英文改为按当前配置触发新运行，属于后续构建待验收文案，不声称 v4 已包含。

## 共享项目、筛选与窄屏

普通 Owner 及共享成员分别通过 `phase1-other` 的“共享项目”入口看见唯一共享仓库 `phase1-parent/child/deep/ancestor-ci`。本树默认 PR 列表为 0；切换“共享给本群组的项目”后为 1，仓库选项只包含该共享项目，没有兄弟私有仓库。

390×844 真实浏览器截图确认导航正常换行、筛选和结果可操作；DOM 只读测量视口宽与文档宽均为 390，没有横向溢出。测试后已恢复默认视口。

## 制品协议和原生 UI

OCI/Generic 的发布、下载、共享上限、撤销、字节一致性及首错记录见[客户端证据](package-client-acceptance-evidence.md)。父代理已从深层群组“制品”入口进入原生列表和 Generic 详情，看到 Developer 发布、三条 Generic/OCI 包、协议所用的原生所属组织路径及下载入口。临时群组共享通过 UI 创建和撤销，不以数据库补授权。

该 Generic 样本是已支持协议的代表性验证；用户实际最常用协议仍待确认，不能把该样本冒充已确认的业务协议。

## 允许的群组移动与范围失效

普通 Owner 在群组 14 设置中将空叶子组从 `phase1-parent/child/deep/leaf` 移至 `phase1-parent/sibling/leaf`。预览准确展示离开 3 条成员来源，离开 deep 的 1 个变量、child 的 1 个 Runner 和 1 个 Hook；新增 sibling 来源为零。本次没有仓库、专属 Runner、密钥和活跃任务，因此为一期允许组合。

确认后当前路径、面包屑及成员页刷新保留。原生本组 Owner 仍保留，已离开的祖先 Developer/Reporter 不在有效成员表。使用真实 API 请求佐证：Owner 200，Developer 和 Reporter 均 404。旧路径由 Owner 打开时解析到新规范路径；完整旧路径无权浏览器矩阵仍未执行。随后通过同一预览及确认流程移回原父组，恢复原测试层级。

叶子组归档后创建入口消失，设置页显示“已归档”和恢复入口。该归档样本没有下级项目，不能证明仓库消费者或独立归档恢复语义。页面仍有旧版未经本轮核实的 GitLab 19.3.2 归属表述，父代理已移除版本声明，保留明确的本系统统一恢复后代行为。群组概览缺少归档标记的问题已补模板，等待下一构建实际回显。

## 尚未完成

本记录仅覆盖上述真实样本。完整审批规则、邀请/申请、多来源/到期的全矩阵、artifact 客户端、Release/LFS 新修复、跨协议移动/改名、重启恢复等仍按操作矩阵继续验收。Linux amd64 目标性能环境未提供；80% 人工活跃时间和次数的实测未完成。一期交付仍未完成。
