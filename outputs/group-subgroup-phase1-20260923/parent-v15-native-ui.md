# 第十五批：原生组织设置真实界面验收

日期：2026-09-24。本记录区分 v14 修复前红例与 v15 同对象复验；仅明列子项通过，不代表一期全矩阵。

## 环境与样本

- 本机隔离站点 `http://127.0.0.1:13043`，运行 v14；未修改生产数据。
- 通过正常 CLI 新建合成账号 `phase1-v15-owner`（用户 21）、`phase1-v15-outsider`（用户 22）；两者 `is_admin=0`。凭据只保存在工作区外私有测试目录，不进入报告。
- 普通 Owner 从顶部创建菜单新建私有根群组 23 `phase1-native-v15`，从群组首页创建私有子群组 24 `native-child`。未用数据库写入补配置。
- 原有浏览器遗留阻塞已经解除。实际点击群组链接可以导航，重新登录后继续使用真实界面。

## 修复前真实操作

1. 从子群组首页进入“设置 → 原生群组设置”，更新描述为“一期原生设置验收：父级 Owner 编辑子群组资料并刷新核对。”，保存成功；刷新后描述仍在。此为资料编辑固定正例，不代表完整角色矩阵。
2. 从原生设置侧栏进入 Applications，创建 `phase1-v15-local-app`（应用 5），回调只指向本机测试接收器。未向应用授予任何用户授权。新生成的测试密钥没有写入报告或原始状态文件。
3. 从同一侧栏进入 Blocked users，屏蔽合成无关用户并填写“一期隔离屏蔽验收”；页面显示账号、备注及解除按钮。
4. 从群组设置预览并执行归档。预览为 1 个群组、0 个项目，归档后首页显示“已归档”，创建入口消失。
5. 经正常设置导航回到应用 5，改名为 `phase1-v15-archived-edit-red` 并保存。页面仍显示“已归档”，同时提示应用更新成功；刷新后名称保留。保存回跳还使用内部组织别名 `g_79e96e61a4f1406f949bba01e6452236`，而非子群组完整路径。
6. 回到 Blocked users，归档期间点击 Unblock，列表变为空。此为修复前行为记录；后续按已冻结的撤销／重新放开语义验证，不能仅因存在此行为推导 P1。

只读数据库佐证见 [parent-v15-native-red-state.json](parent-v15-native-red-state.json)：子群组仍为归档、应用名称确已修改、屏蔽关系已移除。该查询只佐证 UI 结果，不替代真实操作。

## 第十五版同对象复验

构建 SHA-256 为 `cbd72c5fd98cddc82e48c14091b58d8149d24945ada72b33660479051425ac19`，站点仍只监听本机。普通 Owner 21 使用正常导航、保存、刷新完成以下操作：

1. 子群组 24 仍归档，编辑原应用 5 被拒；页面明确显示群组／祖先归档、待删除或忙碌限制。刷新后名称仍为修复前名称，保存回跳使用完整子群组路径，不再暴露内部组织别名。
2. 归档期间可新建屏蔽以收回互动权限；解除屏蔽被拒，提示可操作原因，刷新后屏蔽仍保留。461px 窄屏可见按钮和错误提示正常换行。
3. 群组设置预览并恢复子群组，创建入口恢复。应用 5 改名为 `phase1-v15-restored-green` 成功，刷新仍在；屏蔽备注编辑、刷新保存及解除屏蔽成功，最终列表为空。
4. 子群组原生设置的“操作审计”显示该 OAuth 修改；再从父群组正常导航进入审计，同样看到应用 5、普通 Owner 21 和成功结果。旧 `user` 作用域缺陷已由实际父级页面反证修复。
5. 退出 Owner、登录非管理员无关用户 22。子组 Applications、Blocked users、父组审计三个已验地址均显示原生 HTML 404。随后回到 Owner，屏蔽管理正常显示。

仅祖先 Owner、当前身份撤销、个人与实例管理员回调由定向服务／HTTP 测试覆盖；本浏览器 Owner 同时具有父子原生 Owners Team，不冒充纯继承来源。v15 浏览器没有执行 OAuth 轮换／删除，不把自动化结果当作它们的 UI 证据。

## 样本身份核对与 CI 准备

只读查询确认用户 21／22 均非实例管理员。Owner 21 同时是父组 23 和子组 24 的原生 Owners Team 成员，因此该浏览器样本只证明普通 Owner 管理闭环，不冒充“仅继承 Owner”正例；仅祖先来源另由服务测试验证。

同一普通 Owner 通过父组页面的创建仓库入口新建私有仓库 21 `phase1-native-v15/ci-event-binding`，初始化 README 成功；随后用该测试账号完成真实 HTTP Git clone。专用 Runner 已注册到父群组 23，工作流准备完成后在 v15 推送及执行，结果见 [CI 实测](parent-v15-ci-ui.md)。所有对象均为本地隔离验收样本，不改变原冻结十仓效率样本。

后续第十六版已补原生屏蔽三类审计并完成实际页面与 PG 复验，见[v16 整合](parent-v16-integrated-validation.md)。本记录保留 v15 当时的边界。
